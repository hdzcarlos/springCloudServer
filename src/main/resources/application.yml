#Component info
server:
  port: 8888

spring:
  #Spring cloud Config Repository
  cloud:
    config:
      server:
        git:
          uri: https://github.com/ggamco/SpringConfig.git

  #Spring properties
  profiles:
    active: dev